# Synthetic CLI release demo

Tiny public log sample for offline smoke tests and packaged archives.
Contains **no private data** — fictional service names only.

```bash
contextdesk --data-dir /tmp/cd-demo import fixtures/cli-release-demo
contextdesk --data-dir /tmp/cd-demo corpus list
contextdesk --data-dir /tmp/cd-demo explore "timeout" --corpus <id>
```
