//! Exact gateway-scoped model identity.
//!
//! Re-exported from the public contract crate so existing
//! `cd_core::model_ref` paths stay source-compatible.

pub use cd_triage_sdk::{ModelRef, ModelRefError};
