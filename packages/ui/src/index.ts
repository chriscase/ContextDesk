/**
 * @contextdesk/ui — design tokens and styled accessible components.
 *
 * Target shape (design handoff §6/§7): normalized token set and the unified
 * Popover/Dialog/Drawer/Splitter/DismissibleLayer/ProvenanceChip primitives.
 * This batch ships only the package identity; tokens land with batch B3.
 */
export const UI_PACKAGE = "@contextdesk/ui" as const;
